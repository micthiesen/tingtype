# Logging + optional error notifications (handled by @micthiesen/mitools).
LOG_LEVEL=info

# Optional: if both are set, fatal errors are pushed to Pushover.
# PUSHOVER_USER=
# PUSHOVER_TOKEN=

# Optional: override the tuning config path (defaults to ./config.toml).
# TINGTYPE_CONFIG=./config.toml
